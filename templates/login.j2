{% extends 'base.j2' %}

{% block title %}Login{%endblock%}
{% block body %}
<div class="card">
    <div class="card-header">Login</div>
    <div class="card-body">
        <p>This system tracks who created what labels, so providing your username (or any unique token, e.g., <code>u{{suggested_random}}</code>) allows us to later calculate agreement.
        Please copy and past the random code <code>u{{suggested_random}}</code> into the user name input. </p>
        <form class="form" method="POST" action="{{ url_for('login') }}">
            <div class="form-group row">
                <div class="col-sm-6">
                    <div class="input-group">
                        <input class="form-control" id="userInput" placeholder="User Name" title="User Name" type="text" value="" name="user" />
                        <input type="hidden" name="destination" value="{{ destination }}"" />
                        <span class="input-group-btn">
                            <input class="btn btn-success" type="submit" value="Log-In" />
                        </span>
                    </div>
                </div>
            </div>
        </form>
    </div>
</div>
{%endblock%}
